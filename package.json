{
  "name": "campanha-solidaria",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "22.x" },
  "scripts": {
    "build": "npm --prefix server run build && node scripts/build-site.mjs",
    "test": "npm --prefix server test",
    "start": "node server/dist/server.js"
  },
  "dependencies": { "dotenv": "^16.4.5", "express": "^4.19.2" },
  "devDependencies": { "@types/express": "^4.17.21", "@types/node": "^20.14.9", "typescript": "^5.5.3" }
}
